repos:
  - repo: local
    hooks:
      - id: check-mcp-adapters
        name: MCP adapter checker
        entry: python tools/check_mcp_adapters.py
        language: system
        types: [python]
        stages: [commit]
